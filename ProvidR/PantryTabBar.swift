//
//  PantryTabBar.swift
//  ProvidR
//
//  Created by Shashank Sharma on 1/21/17.
//  Copyright © 2017 Providr. All rights reserved.
//

import UIKit

class PantryTabBar: UITabBarController {

    override func viewDidLoad() {
        
//        let invenSelected   = UIImage(named: #imageLiteral(resourceName: "Inventory"));
//        let invenUnselected = UIImage(named: #imageLiteral(resourceName: "Inventory Filled"));
        
//        self.tabBar.items?[0].selectedImage = UIImage(named: invenSelected)?.withRenderingMode(.alwaysOriginal);
//        self.tabBar.items?[0].image = UIImage(named: invenUnselected)?.withRenderingMode(.alwaysOriginal);
//        
//        let selectedColor   = UIColor(red: 246.0/255.0, green: 155.0/255.0, blue: 13.0/255.0, alpha: 1.0)
//        let unselectedColor = UIColor(red: 16.0/255.0, green: 224.0/255.0, blue: 223.0/255.0, alpha: 1.0)
//        
//        UITabBarItem.appearance().setTitleTextAttributes([NSForegroundColorAttributeName: unselectedColor], for: .normal)
//        UITabBarItem.appearance().setTitleTextAttributes([NSForegroundColorAttributeName: selectedColor], for: .selected)

        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
